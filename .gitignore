# Xcode user state and build products
DerivedData/
build/
*.xcuserstate
xcuserdata/

# SwiftPM (not currently used, kept out if Xcode creates it)
.build/

# macOS metadata
.DS_Store

# Graphify keeps the durable graph/report in git; extraction caches stay local.
graphify-out/cache/
